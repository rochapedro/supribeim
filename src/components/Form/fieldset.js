import styled from "styled-components";

const FieldSet = styled.fieldset`
    border: none;
    width: 100%;
    &:nth-child(1) {
        display: flex;
    }

    &:nth-child(3) {
        display: flex;
    }
`;

export default FieldSet;